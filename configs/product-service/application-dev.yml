# Product Service - dev ortami: tum altyapi lokalde (podman compose) calisir.

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/product_db
    username: crm
    password: crm
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/crm

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

logging:
  level:
    com.etiya.crm.productservice: DEBUG