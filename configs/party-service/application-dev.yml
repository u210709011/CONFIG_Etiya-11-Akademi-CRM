# Party Service - dev ortami: tum altyapi lokalde (podman compose) calisir.

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/party_db
    username: crm
    password: crm
  kafka:
    bootstrap-servers: localhost:9092
  data:
    redis:
      host: localhost
      port: 6379
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/crm

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

keycloak:
  client-id: party-service-m2m
  client-secret: party-service-m2m-secret

logging:
  level:
    com.etiya.crm.partyservice: DEBUG
